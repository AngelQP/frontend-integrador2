describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { TramarsaCoreModule } from './core.module';

describe('TramarsaCoreModule', () => {
  it('should work', () => {
    expect(new TramarsaCoreModule(null)).toBeDefined();
  });
});
*/
